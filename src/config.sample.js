module.exports =  {
    consumer_key: 'insert your consummer key',
    consumer_secret: 'insert your consummer secret',
    access_token: 'insert your access token',
    access_token_secret: 'insert your access token secret',
    timeout_ms: 60 * 1000 // Default timeout for HTTP request (60s)
};
